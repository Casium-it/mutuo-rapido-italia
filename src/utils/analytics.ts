
// Analytics utilities - currently disabled
// This file is kept for potential future use

export const initializeGA = () => {
  // Analytics disabled
};

export const trackPageView = (path: string) => {
  // Analytics disabled
};

export const trackEvent = (action: string, parameters: object = {}) => {
  // Analytics disabled
};
